apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-access-provider.fullname" . }}-test-connection"
  labels:
    {{- include "data-access-provider.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-access-provider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
