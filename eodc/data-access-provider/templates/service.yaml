apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-access-provider.fullname" . }}
  labels:
    {{- include "data-access-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-access-provider.selectorLabels" . | nindent 4 }}
