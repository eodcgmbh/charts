apiVersion: v1
kind: Service
metadata:
  name: {{ include "wg-nginx.fullname" . }}
  labels:
    {{- include "wg-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "wg-nginx.selectorLabels" . | nindent 4 }}
