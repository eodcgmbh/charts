apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wg-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "wg-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wg-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
